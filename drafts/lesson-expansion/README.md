# 后续课程扩展草稿

这里保存了尚未接入课堂正式版的设计表模块、两组探索性测试和当时的工作区改动快照，供后续实现“仿真编程、至少 20 道考核题、考核后的机器人设计和升级动画”时参考。

- `robot-sheet.js`：教材设计表的早期数据结构和页面草稿，尚未接入正式页面。
- `lesson-design.test.cjs`：设计表和障碍环节的草稿测试。
- `expanded-lesson.test.cjs`：后续功能的目标测试，引用了尚不存在的模块，不能作为当前课堂版的通过标准。
- `工作区改动备份.patch`：2026-09-21 从未完成工作区保存的原始差异快照，包含后来已单独发布的地图性能改动；不应直接应用于当前代码。

正式课堂应用只使用仓库根目录中的程序和 `tests/` 目录中的测试。此目录的文件不会被页面加载，也不由 `npm test` 执行。
